let array = ["roberto", true, ["aqui", "é outra"], 3 ] 


//For Each-para cada item do array
// array.forEach(function(item, index){console.log(item, index)})

//Push-joga um item novo no array
// array.push('novo item')

//Pop-remove o último item de um array
// array.pop(); 

//Shift- tira um item do inicio do array
// array.shift()

// Unshift- coloca um item no ínicio do array
// array.unshift("novo item")

// IndexOf- Retorna o index de um item
// console.log(array.indexOf(true))

//Splice- remove um item por um index
// array.splice(0, 3);

//Slice- recorta os item de um array
// let narray = array.slice(1, 3);

let obj = { string: "string", number : 1, boolean: true}
console.log(obj)